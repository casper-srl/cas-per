{
  "name": "cas-per-website",
  "version": "1.0.25",
  "description": "Sito web cas-per.it",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:secrets": "node scripts/check-secrets.mjs",
    "check:tracking": "node scripts/check-tracking.mjs",
    "audit:prod": "npm audit --omit=dev --audit-level=high",
    "check": "npm run check:secrets && npm run audit:prod"
  },
  "dependencies": {
    "astro": "^7.1.6"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
